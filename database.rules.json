{
  "rules": {
    "lobby": {
      "public_waiting": {
        "$mode": {
          ".read": "auth != null",
          ".write": "auth != null"
        }
      }
    },
    "room_codes": {
      "$code": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },
    "matches": {
      "$matchId": {
        ".read": "auth != null",
        ".write": "auth != null && (!data.exists() || data.child('player1').val() === auth.uid || data.child('player2').val() === auth.uid || newData.child('player2').val() === auth.uid)"
      }
    }
  }
}
