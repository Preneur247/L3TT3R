{
  "rules": {
    "lobby": {
      "waiting_matches": {
        "$mode": {
          ".read": "auth != null",
          ".write": "auth != null"
        }
      },
      "seeking": {
        "$mode": {
          ".read": "auth != null",
          ".write": "auth != null"
        }
      }
    },
    "room_codes": {
      "$code": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },
    "matches": {
      "$matchId": {
        ".read": "auth != null",
        ".write": "auth != null && (!data.exists() || data.child('player1Id').val() === auth.uid || data.child('player2Id').val() === auth.uid || (data.child('matchState').val() === 'WAITING' && !data.child('player2Id').exists() && newData.child('player2Id').val() === auth.uid))"
      }
    }
  }
}
